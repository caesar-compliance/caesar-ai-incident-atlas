# Cloudflare bindings — Incident Atlas (dev)

Worker: `incident-atlas-monitor-dev`

| Binding | Type | Status |
|---------|------|--------|
| R2 artifacts | R2 | Optional |
| KV cache | KV | Optional |
| Queue sync jobs | Queue | Optional |

Primary DB: Supabase `caesar-incident-atlas-dev` (`public` schema). Hosted push remains gated separately.
