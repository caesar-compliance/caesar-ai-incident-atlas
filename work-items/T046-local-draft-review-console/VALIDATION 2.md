# Validation Checklist: T046 — Local Draft Review Console and Promotion Gate

- [ ] Check active branch and status (`feat/T046-local-draft-review-console`)
- [ ] Run `python3 tools/validate_dataset.py` (Confirm exactly 12 public incidents exist, none added)
- [ ] Run `python3 tools/validate_pipeline_schemas.py` (Confirm source registry unchanged)
- [ ] Run `node scripts/validate-digests.mjs` (Verify sitemap and static digests unchanged)
- [ ] Run `node scripts/build-rss-feeds.mjs` (Verify RSS unmodified)
- [ ] Run existing mock pipeline validator:
  - [ ] `node scripts/validate-mock-pipeline.mjs`
- [ ] Run new review bundle build pipeline:
  - [ ] `node scripts/build-review-bundle.mjs` (Confirm `tools/review-console/review-bundle.json` successfully compiled)
- [ ] Run new review console safety auditor:
  - [ ] `node scripts/validate-review-console.mjs` (Confirm all containment and safety checks pass successfully)
- [ ] Confirm containment safety:
  - [ ] No review console assets or review bundles reside inside the public root `site/`
  - [ ] No public `INC-0013` file exists
  - [ ] No mock candidate/draft data appears under `site/data/incidents` or in `incident-index.json`
  - [ ] No mock digest data appears in public RSS feeds or digest pages
  - [ ] Source registry entries are all `inactive_draft` and `auto_publish_allowed` is `false`
  - [ ] No scheduled GitHub Actions workflows were enabled
  - [ ] Public root path remains strictly `site/` and Pages workflow uploads only `site/`
